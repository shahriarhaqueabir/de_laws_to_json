# de_laws_to_json dependencies
# Tested on Python 3.14.2
#
# Install:  pip install -r requirements.txt

# --- download_de_laws.py ---
requests>=2.32.5       # HTTP downloads with retry support
tqdm>=4.67.3           # Progress bars
urllib3>=2.6.3         # Pulled in by requests; listed explicitly for the Retry adapter

# --- process_de_laws.py ---
beautifulsoup4>=4.14.3 # XML/HTML parsing (import name: bs4)
lxml>=6.0.2            # Fast XML parser backend for BeautifulSoup
tiktoken>=0.12.0       # OpenAI BPE tokenizer (cl100k_base encoding)

# --- app.py (dashboard) ---
flask>=3.1.0           # Web server for the search dashboard
deep-translator>=1.11.4 # AI translation for bilingual view

# --- dictionary/ (German-English legal dictionary) ---
# Optional: Only required if building the TEI-based dictionary database
# Install: pip install -r requirements.txt  (all dependencies already listed above)
# The dictionary module uses only standard library + existing dependencies:
#   - xml.etree.ElementTree (standard library) - TEI parsing
#   - sqlite3 (standard library) - Database storage
#   - json (standard library) - Data serialization
#   - re (standard library) - Regular expressions
#
# To build the dictionary database:
#   1. Ensure templates/eng-deu.tei exists (FreeDict English-German dictionary)
#   2. Run: python dictionary/parse_tei_dictionary.py
#   3. Run: python dictionary/reverse_dictionary.py
#   4. Run: python dictionary/build_dictionary_db.py --rebuild
